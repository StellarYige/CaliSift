// Local developer tools use loopback. For a phone, configure a reachable HTTPS
// service and the mini program's request/upload domain in WeChat's console.
module.exports = { apiBaseUrl: 'http://127.0.0.1:8000' };
