{
  "name": "calisift",
  "version": "0.4.0-alpha.1",
  "private": true,
  "description": "CaliSift browser-only local calendar",
  "scripts": {
    "test": "npm --prefix web test",
    "build": "python scripts/prepare-web.py && npm --prefix web run build",
    "dev": "npm --prefix web run dev",
    "test:browser": "npm --prefix web run test:browser",
    "package": "python scripts/package-web.py"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {}
}
