# MVP 验证记录

## 已实际完成

- Windows Python 虚拟环境安装、依赖锁定、Git 仓库初始化。
- 146 个 Python 测试通过；语句与分支合并统计覆盖率 **94.49%**，门槛 90%。解析模块 94%、日期时间模块 96%、文件读取 95%、合并模块 98%、API 95%、修正模块 100%。
- 17 个 Node 前端测试通过：并发限制、失败重试、姓名/年份变更后重新解析、文件上限、HTTP 错误、时间线展示、分页、渲染数据隔离、本地恢复/损坏/存储失败、编辑校验、断网保留草稿、取消不改原结果，以及全部上传失败保留旧结果。
- 测试经过实际 `.xlsx`、`.xls`、`.csv` 文件读取；覆盖合并区域、多人姓名、相似姓名、多 Sheet、横纵表、日期班次交叉表、中文日期、Excel 1900/1904 日期系统、跨夜、不同月份表块和公式缓存缺失。
- API 测试启动真实解析子进程；确认多文件部分成功、输入拒绝、超时处理和临时目录清理。
- 本机 `127.0.0.1:8000` 启动真实 Uvicorn 服务；小程序实际页面方法通过 HTTP 桥接上传四份演示表，得到 **5 项安排、4 个日期分组、1 组确定冲突、早班 2 处来源**。
- 修正页调用真实 `/api/review`，把早班从 9 月 7 日改到 9 月 9 日：冲突从 **1 组变为 0 组**，仍保留 **2 处来源**，修正历史记录原日期。重新执行应用初始化，仅依靠存储桥接恢复修正结果，通过。
- 真实上传周期课表，选择其中一条待确认项补充日期及时间；该项进入正式日程，其他待确认项保留，通过。
- Python 测试额外覆盖跨夜显式确认、日期/时间非法、修正合并重复来源、重复与过期记录 ID、连续修正不递归嵌套历史，以及合并结果超过单文件 2,000 条限制后仍能修正。
- 微信开发者工具附带的官方 `wcc.exe`、`wcsc.exe` 成功编译 **三个 WXML 页面及四个 WXSS 文件**；**9 种界面状态**使用真实联调结果生成虚拟 DOM，无运行错误。

本轮执行日期：2026-09-06。运行命令为 `scripts/test.ps1`、`npm run smoke`、`node scripts/compile-mini.js`。实际流程报告保留在 `artifacts/http-smoke-report.json`、`reviewed-smoke-report.json`、`pending-smoke-report.json`，排版入口为 `artifacts/preview.html`。

界面已改为紧凑首页、统一 SVG 星形标识、可恢复结果入口、个人时间线和独立修正表单。已修正、待确认、冲突及保存失败均有文字提示；来源折叠区保留原表线索和修正前内容。以上是实现及编译核验结果，不代替下方尚未完成的视觉验收。

Python 测试中的 `worker.py` 在子进程执行，不计入主进程覆盖采集；其真实行为通过 API 集成测试验证。当前依赖发出两条测试客户端弃用提示，不影响测试通过。

## 尚未通过的验证

**微信模拟器和真机全流程：待有效 AppID。**

此前实际调用开发者工具 CLI 并开启自动化。尽管 `islogin` 返回 `true`，项目窗口仍显示“更改 AppID 失败 touristappid / Error: tourist appid”，页面未进入可操作状态。本轮配置仍为 `touristappid`，尝试捕获项目窗口时开发者工具未运行。尚无有效 AppID，未生成模拟器通过报告。

**手机排版视觉截图：尚未完成。**

本轮重试 Browser 工具仍初始化失败，返回“failed to write kernel assets / 系统找不到指定的路径”。此前启动独立浏览器截图的操作被自动审批拒绝，理由为“blocked by policy”，没有进一步说明；本轮没有换方式绕过该拒绝。因此只记录官方编译和虚拟 DOM 检查通过，不声称视觉核验通过。

本地生成的 `artifacts/devtools-window.png` 仅用于定位 AppID 错误；`artifacts/preview.html` 是编译产物的浏览器排版预览，不是微信截图。

## 有效 AppID 到位后的验收

1. 将有开发权限的 AppID 配置到小程序项目，确认工具中能打开上传页。
2. 保持后端运行，执行 README 中的 `devtools-smoke.js`，检查真实 `wx.uploadFile` 与 `wx.request`。
3. 核对上传、时间线、修正页和来源展开截图；检查窄屏、长文件名、部分失败、无匹配、待确认、字段错误与保存失败状态。
4. 真机从微信聊天选择演示文件，输入“星辰奕歌”，核对 5 项安排、1 组冲突及重复来源。
5. 把早班改到 9 月 9 日，确认冲突消失、两处来源和原日期仍在；关闭后重新打开，从首页继续查看；断网时能看本机结果，保存修正则应报连接失败且保留草稿。
6. 上传周期课表，补充一条待确认安排；清除本机结果时取消一次，再确认一次，核对恢复入口与结果是否正确保留/移除。

`devtools-smoke.js` 已增加修正、真实微信存储恢复和相关截图步骤，但当前未执行通过。文件选择器和日期选择器的返回值由自动化注入，人工验收仍需操作真实原生选择器。

公开发布、域名绑定、AppID 注册和微信审核未执行。
